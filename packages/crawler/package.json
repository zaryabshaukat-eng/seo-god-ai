{
  "name": "@seogod/crawler",
  "version": "0.0.0",
  "private": true,
  "description": "Production-grade Shopify crawler for SEO GOD AI",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@seogod/audit": "*",
    "@seogod/config": "*",
    "@seogod/core": "*",
    "@seogod/database": "*",
    "@seogod/events": "*",
    "@seogod/logging": "*",
    "@seogod/monitoring": "*",
    "@seogod/shopify": "*",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.0.0",
    "vitest": "^3.0.0"
  }
}
