{
  "name": "@seogod/decision-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Deterministic decision engine: converts SEO recommendations into executable, approved, rollback-ready plans",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@prisma/client": "^6.2.0",
    "@seogod/core": "*",
    "@seogod/database": "*",
    "@seogod/events": "*",
    "@seogod/knowledge-graph": "*",
    "@seogod/logging": "*",
    "@seogod/monitoring": "*",
    "@seogod/seo-engine": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.0.0",
    "vitest": "^3.0.0"
  }
}
