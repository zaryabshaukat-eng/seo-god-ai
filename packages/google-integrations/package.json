{
  "name": "@seogod/google-integrations",
  "version": "0.0.0",
  "private": true,
  "description": "Google integrations for SEO GOD AI: Search Console, Analytics 4, PageSpeed Insights, Rich Results and Indexing APIs with OAuth, credential management and incremental sync",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@seogod/core": "*",
    "@seogod/events": "*",
    "@seogod/logging": "*",
    "@seogod/monitoring": "*",
    "@seogod/shared": "*"
  },
  "devDependencies": {
    "@prisma/client": "*",
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.0.0",
    "vitest": "^3.0.0"
  }
}
