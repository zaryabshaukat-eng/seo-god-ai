{
  "name": "@seogod/learning-engine",
  "version": "0.0.0",
  "private": true,
  "description": "Learning engine for SEO GOD AI: feedback collection, outcome analysis, confidence calibration, learned recommendation scoring, RL-style signals and decision-engine-ready historical outcomes",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@seogod/core": "*",
    "@seogod/monitoring": "*"
  },
  "devDependencies": {
    "@seogod/decision-engine": "*",
    "@seogod/observability": "*",
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.0.0",
    "vitest": "^3.0.0"
  }
}
