{
  "name": "@seogod/observability",
  "version": "0.0.0",
  "private": true,
  "description": "Observability engine: records executions, SEO snapshots, immutable change history and alerts for the SEO God AI platform",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@seogod/core": "*",
    "@seogod/crawler": "*",
    "@seogod/events": "*",
    "@seogod/execution-engine": "*"
  },
  "devDependencies": {
    "@prisma/client": "*",
    "@types/node": "^22.10.0",
    "@vitest/coverage-v8": "^3.0.0",
    "vitest": "^3.0.0"
  }
}
